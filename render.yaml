services:
  - type: worker
    name: mqtt-sender
    runtime: python
    repo: https://github.com/Rima-Fahem/mqtt-project
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python afef.py"
